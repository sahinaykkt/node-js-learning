const circleArea = (radius) =>  Math.round(Math.PI * (radius * radius));
const circleCircumference = (radius) =>  Math.round(2 * Math.PI * radius);

module.exports = {
    circleArea,
    circleCircumference
};